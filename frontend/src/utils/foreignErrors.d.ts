export function isInjectedPerformanceMonitorError(
  message: string,
  error?: unknown,
  filename?: string,
): boolean;
export function isBenignWindowError(message: string, error?: unknown, filename?: string): boolean;
